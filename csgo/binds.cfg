unbind 1; unbind 2; unbind 3; unbind 4; unbind 5; unbind 6;
bind mouse5 prwep;
bind mouse4 sewep;
bind 3 mewep;
bind 4 bomb;
bind 5 bomb;
bind z henade;
bind f flash;
bind c smoke;
bind 6 decoy;
bind x fire;
bind v zeus;
bind g drop;
bind y health;

bind q player_ping;

bind w +for;
bind a +lef;
bind s +bac;
bind d +rig;
bind ctrl +dk;

bind mouse3 "+mrad";

bind tab "+scoregraph";
bind enter "+jumpthrow";
bind l "-attack; -attack2"; //no jump for med throws
bind ` toggleconsole;
bind = toggleconsole;
bind downarrow "clear; showconsole; ping";

bind mouse1 +shoot1;
bind mouse2 +shoot2;

bind b buymenu;

bind mwheelup radarcl;
bind mwheeldown radarfr;
